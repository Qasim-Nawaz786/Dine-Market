import { StaticImageData } from "next/image"

export type lastproduct = {
    name: string,
    points  : string,
points1  : string,
points2 ? : string,
points3 ? : string,
points4 ? : string,
id ?: number,
image : StaticImageData,
title : string,

}